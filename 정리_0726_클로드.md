# 정리_0726_클로드

작성예시 형식으로 프롬프트와 답변을 기록합니다.

### 1. (기록 설정)

#### 원문 프롬프트

```text
지금부터 하는 프롬프트들과 그에 대한 너의 대답을 한 개도 빠지없이 마크다운 문서로 정리해줘. 내가 기록을 그만하라고 할 때까지 자동으로 업데이트 해줘
파일명 정리_0726_클로드
```

#### 답변

지금부터의 모든 프롬프트와 답변을 이 파일에 자동으로 기록합니다. 중단을 요청하시면 기록을 멈추겠습니다.

### 2. (프론트엔드, 스플레쉬/로그인 화면 수정)

#### 원문 프롬프트

```text
스플레쉬 화면 중앙에 재수없수 텍스트를 재수없수 흰색 로고로 변경
로그인 화면 맨하단의 인강 홈으로 버튼 삭제
```

#### 답변

- [app/page.tsx](app/page.tsx)의 `Splash` 컴포넌트에서 `<Brand inverse />` 텍스트 워드마크를 `/logo-final-white.png` 흰색 로고 이미지로 교체하고, [app/globals.css](app/globals.css)에 `.splash-logo` 스타일을 추가.
- 로그인 화면(`Login` 컴포넌트) 하단의 "인강 홈으로" 버튼을 삭제.
- `npm run build`로 프로덕션 빌드 검증 완료.

### 3. (폰트 적용)

#### 원문 프롬프트

```text
public/font 폴더에 두가지 형태의 파일 넣어놨어
```

#### 답변

- `public/font`에 추가된 `PretendardVariable.woff2`, `PretendardVariable.ttf`를 확인.
- [app/globals.css](app/globals.css)에 `@font-face` 규칙을 추가하여 두 파일(woff2 우선, ttf 폴백)을 가변 폰트(font-weight 45~920)로 로드하도록 연결. 기존 `--font-ui` 토큰이 참조하던 `Pretendard` 이름과 매칭되어 전체 UI에 자동 적용됨.
- `npm run build`로 프로덕션 빌드 검증 완료.

### 4. (기록 재개)

#### 원문 프롬프트

```text
프롬프트 정리하는 것도 다시 활성화 시켜줘
```

#### 답변

- 중단 요청 전까지 프롬프트/답변 기록을 다시 자동으로 이어서 정리하기로 함.

(사용자가 기록하지 말라고 명시한 프롬프트 1건은 지침에 따라 생략)

### 5. (페이지별 폰트 사용 정리)

#### 원문 프롬프트

```text
페이지별로 어떤 폰트를 썼느닞 정리해줘
```

#### 답변

- [app/globals.css](app/globals.css)의 전역 규칙이 `font-family: var(--font-ui)`(Pretendard)를 모든 요소에 적용하고 있어, 사실상 전 화면이 Pretendard 하나만 사용하며 크기·굵기만 컴포넌트별로 다름을 확인.
- 유일한 예외로 `.equivalent-formula code`(돈워리 환산 화면의 수식 표기)가 `Consolas` 계열 monospace를 사용.
- 화면별 타이포 사용 표로 정리해 안내.

### 6. (돈워리 인트로 타이포 통일)

#### 원문 프롬프트

```text
성적분석 탭의 첫화면과 같은 레벨에 있는 텍스트는 굵기나 크기를맞춰서 통일감을 주게 해줘
```

#### 답변

- `.converter-intro h1`을 커스텀 `29px/500`에서 `var(--type-heading-lg)`(850/25px)로 변경, `.converter-intro main > p`를 `var(--type-caption)`으로 변경해 `.grades-intro`와 동일한 타이포 토큰을 쓰도록 통일.
- `main` 패딩도 spacing 토큰으로 맞춤.

### 7. (채팅 화면 간격/강조/스크롤바)

#### 원문 프롬프트

```text
ai 채팅방 'AI 도우미 노재수'와 '지금 상담 가능해요' 간 간격 좁히기 
채팅창 화면의 'NO'만 줄바꿈 안될만큼 폰트 키우고 굵게 하고 초록색으로 변경
채팅 길게 입력시 생성되는 스크롤바 제거
```

#### 답변

- `.chat-heading p`의 margin-top을 5px→1px로 줄여 제목/상태 텍스트 간격 좁힘.
- 환영 메시지의 "NO"를 `<span className="chat-welcome-no">`로 분리하고 `font-size: 1.35em`, `font-weight: 900`, `color: var(--brand-green)`, `white-space: nowrap` 적용.
- `.chat-input textarea`에 `scrollbar-width: none` + `::-webkit-scrollbar { display:none }`로 스크롤은 유지하되 스크롤바만 숨김.

### 8. (채팅 입력창 포커스 색 질문 및 커스텀)

#### 원문 프롬프트

```text
채팅입력창 클릭시 생성되는 주황색 상자 테두리 색상 뭐야?
```

```text
브랜드 그린 말고 다른 하늘색 생상으로 커스텀해줘
```

#### 답변

- 코드에 지정된 색이 아니라 브라우저 기본 포커스 아웃라인임을 설명.
- `.chat-input textarea`에 `outline: 2px solid transparent`를 기본값으로 두고 `:focus`/`:focus-visible`에서 하늘색(`#38bdf8`)으로 전환되는 커스텀 아웃라인 적용.

### 9. (성적분석 탭 다중 수정: 세그먼트 텍스트/원형바/보통 기준/슬라이딩 카드 색)

#### 원문 프롬프트

```text
성적분석탭 세그먼트바 성적 추이 텍스트 사라짐 개선
안정성 점수의 원형바의 색상 그린으로  변경 및 과목별 안정성 바와 같이 55점 외에는 연한 회색으로 설정하고 구분선 넣기
안정성 점수의 '보통' 은 어떤 기준으로 바뀌고 색상도 같이 바뀌는지 알려줘
추천학습방향의 슬라이딩 카드 색상 추천 부탁
```

#### 답변

- 세그먼트바 "성적 추이" 버튼에 누락됐던 텍스트 복구.
- `.stability-donut`을 실제 55% 비율의 `conic-gradient`(그린 채움 + 연한 회색 트랙)로 교체하고 `report-bar`와 같은 방식의 4분할 구분선(`repeating-conic-gradient`) 추가.
- '보통' 배지가 실제로는 완전히 하드코딩된 정적 텍스트이며 점수와 연결된 기준 로직이 없음을 설명.
- 추천학습방향 카드(1순위 영어/2순위 수학) 배경·아이콘 색을 과목 고유 색(영어=블루, 수학=틸) 기준으로 재배색 제안 및 적용.

### 10. (과목별 안정성 라벨 기준 질문)

#### 원문 프롬프트

```text
과목별 안정성의 경우 보통 보완 높음과 같은 기준 알려줘
```

#### 답변

- `rows` 배열의 라벨이 점수와 무관한 완전 하드코딩 문자열임을 재확인하고, 데이터 패턴상 우연히 들어맞는 구간(50 미만/50~64/65 이상)을 역추정해 설명.

### 11. (새 기준 추천 요청)

#### 원문 프롬프트

```text
새로 기준을 세우고 싶은데 추천해줘
```

#### 답변

- 기존 `--color-insight-positive/warning/danger` 토큰을 재사용하는 3단계 기준(60↑ 높음/그린, 45~59 보통/앰버, 45미만 보완/레드)을 제안.

### 12. (기준 확정 및 명칭/적용 범위 지정)

#### 원문 프롬프트

```text
안정성 점수와 과목별 안정성의 점수별 명칭과 기준
과목별 안정성의 경우 등급에 상관없이 과목 고유의 색상으로 표시, 안정성 점수의 경우 그린, 앰버, 레드로 변환
탄탄 : 60점 이상
원활 : 45~59점
보완 : 45점 미만
```

#### 답변

- `stabilityTier()`/`stabilityRingColor` 헬퍼 추가.
- 안정성 점수(전체 점수, 원형바+배지): 라벨·배지색·원형바 채움색을 모두 점수 기준으로 동적 계산.
- 과목별 안정성: 라벨 텍스트만 동일 기준으로 계산하고, 색상은 과목 고유 색 유지.

### 13. (배지 텍스트 색 통일 / 카드 줄바꿈 / 데이터 연동 여부 점검)

#### 원문 프롬프트

```text
과목별 안정성의 경우 원활과 같은 뱃지 텍스트 색은 일괄적으로 회색 적용
추천학습방향 카드 내 텍스트는 어떤 텍스트가 들어와도 자동적으로 문맥에 맞게 줄바꿈되도록 수정
성적분석 탭의 모든 요소들 백분위 65 부터 추천학습방향 리포트까지 나중에 사용자의 성적이 달라질 때마다 달라질 수 있는지 확인해줘
```

#### 답변

- 과목별 안정성 라벨(`<em>`) 색을 `var(--muted)` 회색으로 통일, 점수 숫자만 과목색 유지.
- `.focus-priority-copy strong`/`p`에 `overflow-wrap: anywhere` 추가.
- 성적분석 탭 전 요소를 감사한 결과, 그래프/막대/도넛 등 좌표 계산 요소는 데이터 연동돼 있으나 백분위 63, 예상범위 56~70, 평균 2.15등급, 48점 기준선, 우선순위 카드 텍스트 등은 데이터와 분리된 하드코딩임을 확인해 보고.

### 14. (하드코딩 텍스트를 실데이터 기반으로 연결)

#### 원문 프롬프트

```text
그러면 목데이터가 바뀌어도 다 고정되어 있는거야?
```

```text
그렇게 고쳐줘
```

#### 답변

- 어떤 요소가 데이터에 연동되어 있고 어떤 요소가 분리된 하드코딩인지 구분해 설명.
- `percentileTrend`, `percentileDropThreshold`, `averageGrade`(등급 환산 함수 포함), `weakestSubject`, 우선순위 카드 정렬 로직 등을 추가해 백분위/등급/기준선/우선순위 카드가 실제 목데이터에서 계산되도록 리팩터링.

### 15. (전체보기 UX: 자동 스크롤/박스 제거/위치 이동)

#### 원문 프롬프트

```text
전체보기를 누르면 저절로 펼쳐진 영역이로 이동
전체보기 버튼 겉에 있는 상자 제거
과목별안정성 아래있는 버튼은 구분선 아래로 이동
```

#### 답변

- 펼침 시 `scrollIntoView`로 자동 스크롤되도록 ref+`useEffect` 추가.
- `.stability-expand-toggle`에서 border/background 제거.
- 버튼을 `.subject-stability` 카드 밖으로 옮겨 카드의 구분선(border-bottom) 아래에 위치하도록 변경.

### 16. (전체보기 버튼 정렬/색상, 스크롤 방향)

#### 원문 프롬프트

```text
전체 보기 버튼 모두가운데로 이동 및 연한회색으로 변경 
성적추이와 집중보완과목 세그먼트 내의 각 전체보기 버튼 클릭시 스크롤 자동으로 맨아래로 내리기
```

#### 답변

- 버튼을 `margin: ... auto`로 가운데 정렬, 색상을 연한 회색(`#b7c0ba`)으로 변경.
- `scrollIntoView`의 `block` 옵션을 `start`→`end`로 변경해 펼쳐진 영역 하단까지 스크롤되도록 수정.

### 17. (스크린샷 피드백: 하단 네비에 가려지는 문제)

#### 원문 프롬프트

```text
아니 전체보기 눌렀을 때 사진의 화면까지 다 보였으면 좋겠어
(성적추이/집중보완과목 확장 화면 스크린샷 2장 첨부)
```

#### 답변

- 고정된 `.bottom-nav`(78px, position:fixed)가 스크롤 종료 지점을 가려 마지막 카드가 하단 네비 뒤에 숨는 문제를 확인.
- `.stability-more`/`.trend-more`에 `scroll-margin-bottom: 96px` 추가.
- 펼침 애니메이션(240ms) 도중 스크롤이 계산되던 문제를 막기 위해 애니메이션 종료 후(260ms 지연) 스크롤하도록 수정.

### 18. (제목 오른쪽 정렬 요청)

#### 원문 프롬프트

```text
'흔들림이 적을수록 좋아요'와 '집중 보완 우선순위' 텍스트 각 제목과 같은 선상의 오른쪽 끝으로 이동
```

#### 답변

- 두 텍스트를 각각 `.card-heading` 플렉스 행에 넣어 "과목별 안정성"/"추천 학습 방향" 제목과 같은 줄, 오른쪽 끝에 배치. `.card-heading`을 `align-items: center`로 변경.

### 19. (텍스트 크기 통일 요청 - 1차, 잘못 이해함)

#### 원문 프롬프트

```text
두 텍스트의 크기를 같게해줘
```

#### 답변

- "과목별 안정성"/"추천 학습 방향" eyebrow를 12px로 축소해 옆의 h2와 크기를 맞춤(→ 다음 프롬프트에서 사용자가 의도와 다르다고 정정).

### 20. (정정: eyebrow 원복 + 4분할 매트릭스 간격)

#### 원문 프롬프트

```text
아니 . "과목별 안정성"/"추천 학습 방향" 이거는 원래 크기로 되돌리고  "흔들림이 적을수록 좋아요" 와 "집중 보완 우선순위" 의 크기 같아지게 해달란 거였어
과목포시션 4분할 화면과 제목 간격 띄우기
```

#### 답변

- eyebrow 크기를 원래(15px)로 되돌림. 두 h2("흔들림이 적을수록 좋아요"/"집중 보완 우선순위")는 이미 둘 다 12px로 서로 같음을 확인.
- `.position-matrix`의 margin-top을 22px→34px로 늘려 제목과의 간격 확보.

### 21. (전체보기 속도 개선 + 간격 일괄 통일)

#### 원문 프롬프트

```text
전체보기 두개다 눌렀을 때 빠르게 이동할 순 없을까 지금 너무 속터져
집중보완과목에서 각 제목들과 그 아래 요소들 간격을 일괄적으로 같은 넓이만큼 띄워줘
```

#### 답변

- 펼침 애니메이션을 240ms→160ms(`var(--motion-fast)`)로 단축, 스크롤 대기시간도 260ms→170ms로 단축.
- 안정성 점수/과목별 안정성/과목 포지션/추천 학습 방향 네 섹션의 제목-본문 간격을 모두 34px로 통일.

### 22. (홈 화면 보험료 데이터화)

#### 원문 프롬프트

```text
홈화면에서 현재 월 보험료와 보험료 재산정까지 값들을 바뀔 수 있는 값들로 바꿔줘
보험료 재산정 날짜는 추후 약관에서 가져올 거고 지금 날짜 기준으로 다음 갱신일까지 얼마 남았는지 계산하게 해줘
```

#### 답변

- 홈 화면의 "현재 월 보험료"·"보험료 재산정까지" 카드를 하드코딩 문자열에서 premiumPlan 목데이터 객체로 분리 (app/page.tsx:290): monthlyAmount, nextDueDate, renewalDate
- renewalDate에는 // TODO: 약관 연동 시 실제 재산정일로 교체 주석을 남겨, 나중에 약관에서 실제 재산정일을 받아오면 이 값만 교체하면 되도록 함
- D-day는 더 이상 고정값이 아니라 실제 계산: daysUntil() 헬퍼가 new Date()(현재 시각)와 renewalDate의 자정 기준 일수 차이를 매번 계산 — 오늘 날짜가 바뀌면 D-day도 자동으로 갱신됩니다
- 금액/날짜 표기도 toLocaleString, formatMonthDay, formatISODate 헬퍼로 변경해 데이터만 바꾸면 화면 문구가 그대로 따라감

### 23. (자동 기록 방식 재확인)

#### 원문 프롬프트

```text
앞으로 프롬프트도 따로 명령이 없어도 자동으로 정리해줘
```

#### 답변

- 별도 요청 없이도 매 프롬프트/답변을 이 파일에 계속 자동으로 이어서 기록하기로 함. 중단 요청 전까지 유지.

### 24. (돈워리 인트로 eyebrow 폰트 위치 질문)

#### 원문 프롬프트

```text
'돈(Money) 걱정은 뚝! Don't Worry' 크기나 굵기는 어디서 바꿔?
```

#### 답변

- `Converter` 컴포넌트의 `.eyebrow` 문구 위치([app/page.tsx](app/page.tsx))와 크기/굵기를 정하는 전역 클래스 `.eyebrow`([app/globals.css](app/globals.css)) 위치 안내. 전역 클래스라 다른 화면 레이블에도 영향을 준다는 점을 주의사항으로 안내(코드 변경은 하지 않음).

### 25. (돈워리 STEP1/STEP2 전면 개편)

#### 원문 프롬프트

```text
돈워리탭 수정사항
-두번째화면: '필수 스킵가능'과 같은 박스들 모든 항목에서 제거, 자녀수 선택안함 항목 추가 및 선택안함 눌렀을 때만 '1명으로 간주해 결과 화면에서 '동생 대학 등록금' 카드가 표시되지 않아요.' 이문구 나오도록 변경, 목표액 미설정 선택안함으로 변경, 항목 순서 저축-자녀수-노후-소득 순으로 변경, 선택안함 안내문구는 화면에 한줄로 표시되도록 줄이기 크기는 유지
 -세번째화면: 총액조정값 박스 제거, '우리동네시세에 맞게 조정' 위치 아이콘 옆으로 하고 아이콘 색상 그린으로 변경
```

#### 답변

- STEP1 모든 항목에서 배지(필수/선택 등) 박스 제거.
- 자녀 수에 "선택안함" 옵션 복원, 선택 시에만 안내문구 노출(그 외 "1명"이어도 문구 없음).
- "목표액 미설정" → "선택안함"으로 명칭 통일.
- 항목 순서를 저축 → 자녀수 → 노후 → 소득으로 재배치.
- 안내문구를 `white-space: nowrap` + `text-overflow: ellipsis`로 한 줄 표시, 폰트 크기는 유지.
- STEP2의 "총액조정값" 박스(슬라이더+범례) 전체 제거, 관련 state·계산식·CSS 정리.
- "우리 동네 시세에 맞게 조정" 텍스트를 아이콘과 한 그룹으로 묶어 나란히 배치, 아이콘 색을 그린으로 변경.

### 26. (환산결과 화면 - 단계별 카드 레이아웃으로 교체)

#### 원문 프롬프트

```text
돈워리 환산결과 화면에서 -만원, 어떻게 줄어들까요 부분 사진처럼 변경해줘
(예상 비용 - 최대 보장 = 최종 준비 3단 박스 + 강조 문구 스크린샷 첨부)
```

#### 답변

- 하이라이트 마커 제거하고 "OOO만원은 어떻게 줄어들까요?" 자연스러운 문장으로 변경.
- 기존 불릿 리스트를 예상 비용 – 최대 보장 = 최종 준비 3단 플랫 박스로 교체(값은 기존 `comparisonAmount`/`coveredAmount`/`finalAmount`에서 계산).
- 하단에 그린 세로 바 강조 문구 "보장을 적용하면 전체 예상 비용의 약 {coveragePercent}%를 덜 준비해도 됩니다." 추가, `coveragePercent`는 (최대 보장 ÷ 예상 비용) × 100으로 계산.

### 27. (슬라이딩 카드 색상/아이콘/구분선 1차 수정)

#### 원문 프롬프트

```text
돈워리탭 환산결과 화면 수정사항
4개의 슬라이딩 카드 브랜드 컬러와 어울리는 색상으로 변경
슬라이딩 카드 안의 아이콘을 감싸는 흰색박스 제거 및 아이콘 자체 색상 흰색으로 변경
1910만원 어떻게 줄어들까요 위에 구분선 추가
```

#### 답변

- 반투명 무지개색 카드 배경을 브랜드 그린 계열 솔리드 색(그린/그린다크/그린딥+블루 액센트)으로 교체.
- 아이콘 감싸던 연한 배경 원 제거, 아이콘·텍스트를 흰색으로 변경(카드가 진한 솔리드 배경이므로).
- "OOO만원 어떻게 줄어들까요?" 섹션 위에 `border-top` 구분선 추가.

### 28. (제목 통일/카드 톤 재조정/간격/강조 아이콘/최종카드 색)

#### 원문 프롬프트

```text
돈워리탭 환산결과 화면
- '1,910만원, 어떻게 줄어들까요?'를 '1,910만원, 우리 집엔 얼마나 클까요?'와 동일하게 크기 및 형광펜 색상 변경하고 두 문구 다 볼드처리
- 슬라이딩 카드 투명도 40으로 설정 색상은 하늘, 노랑, 보라, 핑크인데 전체적으로 잘 어우러지는 색상으로 선정
- 슬라이딩카드 내 '우리 집 월 저축액'과 같은 제목과 '이만큼 저축해야 모을 수 있는 금액이에요.'같은 소제목 사이의 간격 좁히고 구분선 위로 좀만 올리기
- '보장을 적용하면 전체 예상 비용의 약 73%를 덜 준비해도 됩니다.' 이 문구앞 초록색 선 제거 및 전구같인 이모티콘 눈에 잘띄는 색상으로 변경해서 추가
-최종준비 카드는 맨상단 카드의 '~만원' 색상이랑 동일하게 변경하고 투명도 높이기
```

#### 답변

- 두 헤딩의 폰트 크기(18px)·굵기(900)·형광펜(mark) 색을 통일.
- 슬라이딩 카드를 하늘/노랑/보라/핑크 40% 투명도 배경으로 재배색, 텍스트/아이콘은 `currentColor` 상속으로 각 색의 진한 톤 적용.
- 제목-소제목 간격 및 구분선 위치를 좁혀서 위로 당김.
- 강조 문구의 초록 좌측 바 제거, 앰버색 Lightbulb 아이콘을 문구 앞에 추가.
- 최종 준비 카드 배경을 브랜드 그린 톤으로, 금액 텍스트를 맨 위 카드와 동일한 브랜드 그린으로 변경.

### 29. (슬라이딩 카드 투명도 재확인 및 조정)

#### 원문 프롬프트

```text
스라이딩 카드 투명도 40% 맞아? 하나도 안투명한데
```

#### 답변

- 실제로 alpha 0.4가 적용돼 있지만, 카드 뒤가 단색 배경이라 비쳐 보이는 효과가 없어 "불투명해 보이는" 것임을 설명.
- 사용자가 선택한 방향(불투명도를 낮춰 더 연하게)에 따라 4개 카드의 alpha를 0.4 → 0.2로 낮춤.

### 30. (환산결과 수식 폰트 질문)

#### 원문 프롬프트

```text
돈워리 탭 환산결과화면 수식에 쓰인 글시체 뭐야?
```

#### 답변

- `.equivalent-formula code`에 지정된 `Consolas, "SFMono-Regular", monospace`임을 확인해 안내(이 화면에서 유일하게 Pretendard가 아닌 폰트).

### 31. (수식 폰트 변경 + 구분선/카드/간격/뒤로가기 정리)

#### 원문 프롬프트

```text
돈워리탭 환산결과 화면 수정사항
수식 글씨체 프리텐다드로 변경
슬라이딩 카드 내 구분선 안보임 잘보이게 만들기
하단 구역의 예상비용 카드, 최대보장 카드, 최종준비카드 모두 흰색배경에 테두리 있는 상자로 변경
처음부터 계산하기 버튼 위의 영역이랑 간격 넓히기
두번째 화면과 환산결과 화면의 뒤로 가기 버튼 삭제, 세번째 화면 뒤로가기 '이전 화면'으로 변경
```

#### 답변

- 수식 코드 폰트를 monospace → `var(--font-ui)`(Pretendard)로 변경.
- 슬라이딩 카드 구분선을 `color-mix(in srgb, currentColor 35%, transparent)`로 변경해 각 카드 색 위에서도 잘 보이게 함.
- 예상비용/최대보장/최종준비 3개 카드를 모두 흰 배경 + 테두리 박스로 통일.
- "처음부터 다시 계산하기" 버튼 위 여백을 18px → 36px로 확대.
- STEP1과 결과 화면은 뒤로가기 버튼 제거(로고로 대체), STEP2는 라벨을 "이전 화면"으로 변경.

### 32. (로고 교체 및 중복 파일 정리)

#### 원문 프롬프트

```text
엥 우리로고 이거 아닌데 public 폴더에 있는거로 해줘
```

```text
public폴더에도 로고초록버전 화이트 버전이 각각 2개씩 있는데 이걸 하나씩만 남겨주고 그에 맞게 코드도 수정해줘
```

#### 답변

- TopBar의 텍스트 워드마크(`Brand` 컴포넌트)를 삭제하고 `/logo-final-dark.png` 실제 로고 이미지로 교체, 관련 `.brand-wordmark` CSS도 정리.
- `jaesoo_logo.png`(`logo-final-dark.png`와 동일 파일), `logo_final_white.png`(`logo-final-white.png`와 동일 파일) 중복본 2개를 삭제. 코드에서는 이미 하이픈 이름만 참조하고 있어 코드 수정은 불필요했음.

### 33. (핵심 데이터 DB 연동 대비 - 중앙화)

#### 원문 프롬프트

```text
학생이름, 모의고사 성적들, 보험료 결제 내역, 결제 수단, 가입 상품 및 가입 날째, 그에 따른 보장상한 모두 나중에 db에서 가져와서 변경할 수 있게 만들어놔줘
```

#### 답변

- `studentProfile`(이름/학년), `policyInfo`(상품명/등급/가입일/보장한도/납입일), `paymentMethod`(카드사/명의구분/뒤4자리) 목데이터 객체를 새로 만들고 DB 연동 TODO 주석을 남김.
- 홈/마이페이지/보험료 상세/청구 플로우 등 흩어져 있던 학생이름·결제수단·가입정보 하드코딩을 전부 이 객체 참조로 교체.
- 화면마다 제각각이던 보장 한도 표기("1,404만원" 등 8곳 이상, 돈워리 탭의 1,400과도 불일치)를 `policyInfo.coverageCapManwon` 하나로 통일.
- 기존에 이미 모듈 상수였던 모의고사 성적 데이터(`subjects`/`percentileTrend`/`gradeHistory`/`gradeSubjectHistory`)와 결제 내역(`paymentHistory`)에도 DB 교체 대상 TODO 주석 추가.
