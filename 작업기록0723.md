# 작업 기록 (2026-07-23)

> 오늘 세션에서 입력한 프롬프트와 그에 대해 처리한 내용을 프롬프트를 입력할 때마다 자동으로 기록합니다.

---

### 1. "우선 작업기록0723 마크다운 문서를 생성하고 그 안에 1. 내가 쓴 프롬프트 2. 작업한 내용(수정한 파일, 수정사항 등) 이 두가지를 오늘 내가 따로 요청하지 않아도 내가 프롬프트 입력할 때마다 자동으로 기록해줘"
`작업기록0723.md` 문서를 새로 생성했습니다. 앞으로 이 세션에서 프롬프트를 입력할 때마다 (1) 프롬프트 원문과 (2) 실제로 수정한 파일·수정 내용을 이 문서에 자동으로 추가합니다.

---

### 2. (이미지 2장 첨부) "아래는 성적분석 탭 수정사항이야. 
## 1. 성적추이 세그먼트의 왼쪽 테두리가 빨간색인 카드, 왼쪽 테두리가 초로색인 카드를 이미지와 같이 디자인을 변경해줘 
## 2. 집중보완과목 세그먼트의 제목을 아래와 같이 바꾸고 각 제목에 맞게 포함된 내용들을 변경해줘. 
# 성적꾸준함점수-> 안정성 점수 / 과목별 변동성-> 과목별 안정성 / 사분할 차트 y축 제목 -> 안정성"

`frontend/src/App.jsx` 수정.
- **성적추이 세그먼트**: "보험 보장 기준선" 카드를 흰 배경+왼쪽 빨간 테두리에서 옅은 빨강 배경 전체 채우기(`#FCE9EC`, 텍스트 `#9A3B3B`/강조 `#C0304A`)로 변경. "고3 성적 내려가도 걱정 마세요" 카드를 흰 배경+왼쪽 초록 테두리에서 옅은 초록 배경 전체 채우기(`#E4F5EC`, 제목 `#0B8F58`, 본문 `#3F5B4E`)로 변경.
- **집중보완과목 세그먼트**: "성적 꾸준함 점수"→"안정성 점수"로 제목 변경, 본문의 "꾸준하다는 뜻" 문구를 "안정적이라는 뜻"으로 수정. "과목별 변동성"→"과목별 안정성"으로 제목 변경하면서 표시 로직도 변동성(숫자가 클수록 나쁨)에서 안정성(숫자가 클수록 좋음, `100-변동성`)으로 반전시키고, 낮음/높음 라벨도 함께 반전, 부제·각주 문구도 안정성 기준으로 수정. 사분할 차트 Y축 라벨 "꾸준함 ↑"→"안정성 ↑", 축 설명 문구의 "성적 꾸준함"도 "안정성"으로 수정.

---

### 3. (이미지 1장 첨부) "아래는 돈워리 탭 수정사항이야 
## 1. 첫화면 아이콘을 첨부한 이미지로 변경해줘. 아무 효과도 넣지말고 그대로 흰배경에 붙여줘 
## 2. 두번째 화면에서 '선택안함' 박스들을 흰색으로 채워줘. 테두리 색깔은 그대로 유지해줘. 
## 3. 환산결과가 나오는 화면의 두 번째 박스 안의 텍스트를 '보험이1,400만원함께 부담해요' 이문장 아래 텍스트는 밖으로 빼서 맨 아래 '환산은 이해를 돕기 위한 참고 계산이에요. 실제 가계 상황과 다를 수 있어요.' 뒤로 옮겨줘 
## 4. 중간에 '이 금액'을 '{1910만원}'으로 바꿔줘. {} 이 안은 사용자가 선택한 재수비용으로 항상 바뀌어야해 
## 5. 환산결과를 알려주는 5개의 카드들은 바로 보여주지 않고 사용자가 '결과보기'와 같은 버튼을 누르면 보여주는 방식으로 바꿔줘 
## 6. '입력수정' 버튼 크기가 지나치게 얇아 세로폭을 넓게 해줘"
`frontend/src/App.jsx`, `frontend/public/` 수정. (처음엔 첨부 이미지 파일을 디스크에서 찾지 못해 사용자에게 저장을 요청했고, 이후 `frontend/public/가계부3.png`로 저장된 것을 확인해 `donworry_icon4.png`로 이름을 바꿔 적용, 이전 `donworry_icon3.png`는 삭제.)
- **첫화면 아이콘**: `IMG_DONWORRY` 경로를 새 이미지(`donworry_icon4.png`)로 교체. 크롭·마스킹 등 추가 효과 없이 원본 그대로 흰 배경 위에 표시.
- **입력 화면 '선택안함' 박스**: 배경을 초록 틴트(`rgba(11,143,88,0.12)`)에서 흰색(`#fff`)으로 변경, 초록 테두리·초록 글자색은 유지.
- **환산결과 화면**: 초록 박스는 "보험이 {보장액} 함께 부담해요" 문장까지만 남기고, 그 아래 있던 "현물(바우처)/현금 지급 비율" 문구와 "이 시뮬레이션은…" 면책 문구는 박스 밖으로 빼서 맨 아래 "환산은 이해를 돕기 위한 참고 계산이에요." 뒤에 순서대로 재배치.
- **"이 금액" 문구**: `{vm.costTotalLabel}`(사용자가 고른 재수비용 총액)로 바꿔 항상 실제 선택 금액이 표시되도록 수정.
- **5개 환산 카드**: `showResultCards` 상태를 새로 추가해 기본은 "결과 보기 ›" 버튼만 노출, 클릭 시에만 5개 카드가 나타나도록 변경. 뒤로가기·입력수정 시 상태 초기화.
- **입력 수정 버튼**: 높이 48px → 64px로 확대.

---

### 4. "현재 돈원리탭 첫화면에서 상단탭과 재수비용 제목과의 간격, 하단탭과 '1분만에 계산하기' 버튼과의 간격이 얼마야?"
코드 수정 없이 값만 확인해 안내: 상단(로고 바)~제목 간격 = 컨테이너 `padding-top:18px` + 제목 `margin-top:32px` = 50px. 하단 탭바~버튼 간격 = 컨테이너 `padding-bottom:130px` + wrapper `margin-bottom:-98px` = 32px.

---

### 5. "돈워리 탭 변경사항이야 
## 1. 환산결과 화면에서 초록색 상자의 크기를 키워주고 그 안의 글자 크기를 전체적으로 키워줘. 
## 2. 상단 카드의 '1년동안 발생하는 재수 비용은 얼마일까요?' 이 텍스트의 굵기를 줄여주고 크기를 키워줘 
## 3. '1,910만원, 우리집엔 얼마나 클까요?' 이 문장에서 1,910만원에 형광펜을 칠해서 강조해줘 
## 4. 상단(로고 바) ↔ '재수 비용' 제목을 55로 늘리고 하단 탭바 ↔ '1분만에 계산하기' 버튼을 20으로 늘려줘. 
## 5. 입력하기 버튼의 크기가 안키워졌는데 왜그런지 알려줘"
`frontend/src/App.jsx` 수정.
- **초록 박스(환산결과)**: `padding:15px 16px`→`22px 20px`, `border-radius:22px`→`26px`로 확대하고 내부 모든 텍스트(제목/뱃지/보장없이·내 부담 값/화살표/보험 부담 문구/현물·현금 문구) 폰트 크기를 전체적으로 한 단계씩 키움.
- **상단 카드 제목**: "1년동안 발생하는 재수 비용은 얼마일까요?" `font-size:12px;font-weight:700`→`16px;font-weight:500`으로 변경.
- **"{금액}, 우리집엔 얼마나 클까요?"**: 금액 부분만 `<span>`으로 감싸 `linear-gradient(to top, #FFEB3B 44%, transparent 44%)` 배경으로 형광펜 밑줄 효과 적용.
- **상/하단 간격**: 제목 `margin-top:32px`→`37px`(총 55px), 버튼 wrapper `margin-bottom:-98px`→`-110px`(총 20px).
- **'입력 수정' 버튼이 안 커지는 원인 확인 및 수정**: 이 버튼이 속한 스크롤 영역은 `display:flex;flex-direction:column`인데, 이 버튼에만 다른 CTA 버튼들과 달리 `flex:none`이 빠져 있어서 콘텐츠가 길어지면 기본 `flex-shrink:1`에 의해 지정한 `height:64px`가 다시 눌려 작아졌던 것. `flex:none`을 추가해 실제로 커지도록 수정.

---

### 6. "1. 하단 간격 40으로 늘려줘 2. 상단 카드 제목 14로 줄여줘 3. 형광펜으로 칠한 금액 왼쪽의 이모티콘 지워줘"
`frontend/src/App.jsx` 수정.
- 하단 탭바~버튼 간격: wrapper `margin-bottom:-110px`→`-90px`(총 20px→40px).
- 상단 카드 제목 "1년동안 발생하는 재수 비용은 얼마일까요?" `font-size:16px`→`14px`.
- 형광펜 강조된 금액 문구 앞의 📊 이모티콘 삭제.

---

### 7. 
## 1. 입력수정 버튼 높이 32로 줄여줘 
## 2. 4개의 카드 섹션을 개별 요약형 아코디언(Accordion) 패턴으로 개편해줘 (헤더: 아이콘+제목 / 강조 수치+화살표, 바디: 프로그레스 바 / 안내 문구 / 수식 설명 추가) 
## 3. 두번째 화면의 '여기 입력하는 정보는'으로 시작하는 문장을 다음버튼 위로 옮기고 상자는 제거해서 텍스트만 남겨줘 
## 4. 세번째 화면에서 재수유형 선택과 우리 동네시세 조정하는 부분은 두번째 화면에서처럼 박스로 구분해줘"
`frontend/src/App.jsx` 수정.
- **입력 수정 버튼**: 높이 64px → 32px.
- **4개 환산 카드 → 아코디언**: 카드별 `id`(save/tuition/retire/income)와 `openCards` 상태를 추가해 개별로 펼치고 접을 수 있게 변경. 헤더는 항상 노출(원형 아이콘+제목, 우측에 강조 수치+회전하는 화살표(⌄)), 바디(펼쳤을 때만)에는 기존 프로그레스 바·안내 문구에 더해 "{총액} ÷ {기준값} = {결과}" 형태의 수식 설명을 새 필드(`formula`)로 계산해 추가. 계산에 쓰인 실제 값(월 저축액, 월 소득, 학기당 등록금, 노후 목표액)을 `saveValLabel`/`incomeValLabel`/`semesterCostLabel`/`retireGoalLabel`로 새로 노출.
- **"여기 입력하는 정보는…" 문구**: 박스(테두리·배경)를 제거해 일반 텍스트로 바꾸고, 입력 화면 맨 아래 "다음" 버튼 바로 위로 이동.
- **재수 유형 선택 / 우리 동네 시세 조정**: 입력 화면의 카드 스타일(흰 배경+연한 테두리+그림자, 이모지+제목 헤더)을 그대로 적용해 각각 박스로 구분.

---

### 8. (토글 참고 이미지 1장 첨부) 
## "1. '서울 학군지','서울비학군지', '수도권', '지방', '수도권시세기준(전국 평균과 동일)' 글자의 굵기를 줄여줘. 다른 글자들은 바꾸지마 
## 2. 결과보기 버튼은 없애줘 
## 3. 4개 카드의 수식이 실제로 시행된 수식이 맞는지 확인하고 변경해줘 
## 4. 토글 모양은 첨부한 이미지처럼 수정해줘 닫혀있을 때 화살표가 오른쪽 방향, 열리면 아래를 향하는 방향으로 바꿔줘 
## 5. 초록상자 크기에 맞게 안의 텍스트 크기를 키워주고 상자와 그 아래 텍스트 간의 간격을 넓혀줘"
`frontend/src/App.jsx` 수정.
- **지역/시세 문구 굵기**: 지역 카드 이름(`r.name`)과 하단 안내 문구(`vm.adjLabel`, 예: "수도권 시세 기준 (전국 평균과 동일)")만 `font-weight:700`→`400`으로 낮춤. 다른 글자는 손대지 않음.
- **"결과 보기" 버튼 제거**: 버튼과 이를 위해 추가했던 `showResultCards` 상태·`revealResultCards` 함수를 전부 삭제해 4개 카드가 다시 바로 렌더링되도록 되돌림(카드별 아코디언 펼침/접힘 기능은 유지).
- **카드 수식 검증**: 실제 계산 로직과 대조한 결과, 동생 대학 등록금 카드의 수식에서 학기당 등록금을 355.3만원이 아닌 반올림된 355만원으로 표시하고 있던 오차를 발견해 정확한 값(355.3만원)으로 수정. 나머지 3개(저축액/노후자금/소득) 수식은 실제 계산식과 일치함을 확인.
- **토글 화살표**: 문자 `⌄`를 0/180도 회전시키던 방식에서, 원형 배지 안에 `›` 문자를 넣어 닫힘=0도(오른쪽), 펼침=90도 회전(아래)으로 바뀌도록 수정.
- **초록 상자**: 내부 텍스트 전체 크기를 한 단계씩 키우고(제목/뱃지/보장없이·내 부담 값/화살표/보험 부담 문구/현물·현금 문구), 상자 바로 아래 "{금액}, 우리집엔 얼마나 클까요?" 문구에 `margin-top:16px`을 추가해 간격을 넓힘.

---

### 9. "마이탭 수정사항들이야 
## 1. '내성적이력 전체 보기'를 '내 모의고사 성적 이력 보기'로 변경 및 클릭 시 화면내의 '최근모의고사성적' 박스 투명도 있는 초록색상으로 변경 및 왼쪽 초록색 테두리 제거 
## 2. 스탠다드 써있는 상자 제거 
## 3. 로그아웃 버튼 세로폭 안늘어나는 오류 확인해줘"
`frontend/src/App.jsx` 수정.
- **"내 성적 이력 전체 보기"** → **"내 모의고사 성적 이력 보기"**로 문구 변경. 클릭 시 이동하는 성적 이력 화면의 "최근 모의고사 성적" 박스를 흰 배경+왼쪽 초록 테두리(`border-left:4px solid #0B8F58`)에서 투명도 있는 초록 배경(`rgba(11,143,88,0.08)`)+얇은 초록 테두리로 변경, 왼쪽 굵은 테두리는 제거.
- **마이페이지 메인 화면의 "스탠다드 · 가입일…" 상자**: 통째로 삭제.
- **로그아웃 버튼이 안 커지는 원인**: 이전 "입력 수정" 버튼과 동일한 원인 — 마이탭 화면도 `flex-direction:column`인 스크롤 컨테이너 안에 있는데 로그아웃 버튼에 `flex:none`이 빠져 있어 `height:44px`를 줘도 콘텐츠가 길어지면 다시 눌렸던 것. `flex:none`을 추가해 수정.

---

### 10. "돈워리 탭 수정사항이야 
## 1. 환산결과 화면 내 초록상자의 '1910만원->510만원' 텍스트의 위아래 여백을 살짝씩만 늘려줘 
## 2. 수식에서 나누기가 더하기처럼 보여. 수식의 글자 크기를 키워줘 
## 3. 다음버튼과 우리집기준으로환산하기 버튼의 세로폭을 35로 줄여줘"
`frontend/src/App.jsx` 수정.
- **초록상자 "1,910만원 → 510만원" 비교 행**: 위쪽 `margin-top:18px→22px`, 아래쪽(다음 문구 "보험이 …") `margin-top:12px→16px`로 살짝 늘림.
- **수식(÷) 글자 크기**: 카드 수식 문구 `font-size:10.5px→13px`로 키우고, 너무 촘촘해 보이던 `letter-spacing:-0.2px`도 `0px`로 풀어 나누기 기호가 더하기처럼 안 보이도록 함.
- **"다음" / "우리 집 기준으로 환산하기" 버튼**: 각각 `height:58px`/`min-height:60px+padding:18px` → `height:35px`로 축소(두 버튼 구조를 동일하게 정리).

---

### 11. "ai 채팅창에서 ai 채팅 글자 크기가 너무 작아. 글자크기를 키워주고 양쪽정렬을 자동으로 맞출 수 있도록 정렬해줘"
`frontend/src/App.jsx` 수정. AI 채팅창의 사용자 말풍선·AI 말풍선 모두 `font-size:11.5px→14px`로 키우고, `text-align:justify`를 추가해 줄바꿈되는 문장의 좌우가 자동으로 정렬되도록 함(표(table) 렌더링 부분은 영향 없음).

---

### 12. "돈워리 탭의 환산결과화면에서 4가지 결과 카드의 개별토글을 없애고 제목옆에 '상세보기'와 '간단히보기' 버튼을 만들어줘. 두개 버튼이 아니라 하나의 버튼 안에서 스위치 되는 형식으로 만들어줘"
`frontend/src/App.jsx` 수정. 카드별 `openCards`/`toggleCard`(개별 아코디언)를 전부 제거하고, 단일 상태 `resultDetail`로 교체. "{금액}, 우리집엔 얼마나 클까요?" 제목 옆에 알약 모양 버튼 하나를 추가해 `resultDetail` 값에 따라 라벨이 "상세 보기" ↔ "간단히 보기"로 스스로 바뀌며, 클릭할 때마다 4개 카드의 프로그레스 바·안내 문구·수식 설명이 한 번에 모두 펼쳐지거나 접히도록 변경. 각 카드 헤더의 개별 화살표(chevron)는 제거.

---

### 13. (참고 이미지 2장 첨부) "사진처럼 버튼 내 아이콘(축소/확대 기호)를 추가해줘. 버튼 클릭 시 4개 카드 전체의 상세 콘텐트 영역이 동시에 max-height와 opacity 애니메이션을 거쳐 펼쳐지거나 접혀야해. 간단히 보기 모드 시 카드 간의 수직 간격과 내부 여백을 줄여 전체적인 밀도감을 높여줘."
`frontend/src/App.jsx` 수정.
- **버튼 아이콘**: 확대(상세히 보기)/축소(간단히 접기) SVG 아이콘을 라벨 앞에 추가(라벨 텍스트도 첨부 이미지에 맞춰 "상세히 보기"/"간단히 접기"로 수정), 브랜드 그린 색상 유지.
- **애니메이션**: 각 카드의 상세 영역(프로그레스 바·안내 문구·수식)을 조건부 렌더링 대신 항상 렌더링해두고, `max-height`(0↔200px)·`opacity`(0↔1)·`margin-top`에 `transition`을 걸어 4개 카드가 동시에 부드럽게 펼쳐지고 접히도록 변경.
- **간단히 보기 밀도**: `resultDetail`이 false일 때 카드 사이 세로 간격 `gap:12px→8px`, 카드 내부 패딩 `15px 18px→11px 16px`로 줄여 더 촘촘하게 보이도록 하고 각각에 `transition`을 추가.

---

### 14. "마이탭의 내 모의고사 성적 이력보기 화면 수정사항이야 
## 1. 기본 레이아웃(Slim Accordion List) — 회차별 카드는 접힌 슬림 바 형태, 회차명·시행일자·전체 백분위 및 증감폭·펼치기 화살표 표시 
## 2. 펼침 상태 — 클릭 시 과목별(국어/수학/영어/탐구) 원점수·증감폭·등급, 상승은 초록/하강은 분홍·빨강 색상 강조 
## 3. 상단 툴바 — '전체 펼치기/전체 접기' 버튼, '최근순/과거순 정렬' 토글 버튼"
`frontend/src/App.jsx` 수정. 마이탭 → 내 모의고사 성적 이력 화면(`myIs.gradeHistory`)을 통째로 새로 구성.
- **데이터 모델 확장**: 기존 `examHistory`(과목 점수 나열만 있던 배열)를 회차별 시행일자(`examDates`)·이전 회차 대비 백분위 증감(`percentileDiff`)·과목별 등급(`examGradeOf`, 백분위 컷 기반 근사)·과목별 원점수 증감(`diff`)까지 포함한 구조로 재계산. 정렬 방향은 `examSortDesc` 상태로 관리(최근순↔과거순), 펼침 여부는 회차별 `examOpen` 상태로 관리.
- **슬림 아코디언 카드**: 평소엔 회차명·시행일자·"백분위 66 ▲5" 형태의 백분위+증감·화살표만 보이는 얇은 바. 클릭하면 `max-height`/`opacity` 애니메이션으로 부드럽게 펼쳐지며 과목별 미니 카드(과목명/원점수+증감/등급) 노출. 증감 방향에 따라 상승(초록 `#E4F5EC`/`#0B7A4A`)·하강(분홍빨강 `#FCE9EC`/`#C0304A`) 배경·글자색이 자동으로 바뀜.
- **상단 툴바**: "모의고사 히스토리" 제목 옆에 "최근순/과거순" 정렬 토글과 "전체 펼치기/전체 접기" 토글, 각각 한 버튼이 상태에 따라 라벨을 스스로 바꾸는 방식(직전 돈워리탭 작업과 동일한 패턴).

---

### 15. (스크린샷 첨부) "화면이 이렇게 나오는데 어떻게 된거야" — 카드 하나를 펼치면 다른 회차 행들의 "백분위 XX ▲N" 텍스트가 흐릿하게 깨져 보이는 문제 제보
- 처음엔 Hot Reload(HMR) 잔상으로 추정해 하드 리프레시·개발 서버 재시작을 안내했으나, 사용자가 "두가지 방법 모두 했는데 여전히 같은 문제가 보여"라고 재확인.
- Playwright(headless Chromium)를 설치해 실제로 앱을 로그인 → 마이탭 → 성적 이력 화면까지 직접 조작해 재현에 성공. DOM의 실제 `opacity`/`color`/`transform` 계산값을 확인한 결과 스타일 자체는 전부 정상(검정 글자, opacity 1)이었음 — 즉 코드 로직 버그가 아니라, 스크롤 리스트 안에서 `max-height` 트랜지션을 가진 아코디언 요소가 여러 개 있을 때 생기는 **브라우저 리페인트(rasterization) 버그**였음을 확인.
- **수정**: 각 회차 카드의 펼침 애니메이션을 `max-height` 방식에서 CSS Grid `grid-template-rows: 0fr → 1fr` 방식으로 교체(`frontend/src/App.jsx`). 이 방식은 값을 추측할 필요가 없고 해당 브라우저 리페인트 버그를 우회하는 표준적인 기법. 재현 스크립트로 재검증해 정상 렌더링(모든 회차의 백분위·증감 텍스트가 선명하게 표시)됨을 확인.
- 테스트에 사용한 임시 Playwright 스크립트는 작업 후 삭제.

---

### 16. (참고 이미지 첨부) "사진 처럼 등급 등하락만 표시해주고 등급이 유지된 경우는 회색박스로 변경해줘"
`frontend/src/App.jsx` 수정. 과목별 미니 카드의 색상·증감 표시 기준을 원점수 증감(`diff`)에서 **등급 증감(`gradeDiff`)** 으로 변경.
- 데이터: 과목별로 이전 회차 등급도 함께 계산해 `gradeDiff`(현재 등급 − 이전 등급, 음수면 등급 상승) 추가.
- 표시: 기존에 따로 있던 "▲10점"류 원점수 증감 줄은 제거하고, "{등급}등급" 옆에 등급이 바뀐 경우에만 ▲(상승)/▼(하락) 화살표를 붙임. 등급이 그대로면 화살표 없음.
- 색상: 등급 상승 → 초록(`#E4F5EC`/`#0B7A4A`), 등급 하락 → 분홍빨강(`#FCE9EC`/`#C0304A`), 등급 유지(또는 첫 회차라 비교 불가) → 회색(`#EDEDED`/`#666`)으로 변경(기존 거의 흰색에 가까운 `#F5F6F5`보다 확실한 회색톤).
- Playwright로 재검증해 사진과 동일한 형태로 렌더링되는 것을 확인.

---

### 17. "홈탭의 월별 납입 내역을 가독성이 뛰어난 모던 '영수증 카드' 형태로 제작하고 싶어 
## 1. 상단 헤더(초록배경+영수증 워터마크+체크 배지+타이틀+관리번호) 
## 2. 요약 영역(점선 구분, 좌측 총 납입 합계 모노스페이스, 우측 펄스 도트+전액 정상 납입) 
## 3. 본문 리스트(3열 컬럼 헤더, 각 행 연월/금액/상태, 점선 구분선, 호버 시 배경색 변화)"
`frontend/src/App.jsx`, `frontend/src/index.css` 수정. 홈탭 보험현황 상세 화면의 "월별 납입 내역" 단순 표를 영수증 카드로 전면 개편.
- **데이터**: `payments` 합계(`paymentsTotalLabel`)와 가입일 기반 영수증 관리번호(`paymentsReceiptNo`, 예: `RCPT-20250302-017`)를 새로 계산.
- **상단 헤더**: 초록 그라데이션 배경 위에 옅은 영수증 모양 워터마크 SVG, 중앙 체크 배지(원형), "월별 납입 영수증" 타이틀, 관리번호 표시.
- **요약 영역**: 점선(`border-bottom:1px dashed`)으로 리스트와 구분, 좌측 "총 납입 합계"를 모노스페이스 폰트로 강조, 우측엔 `index.css`에 새로 추가한 `pulse-ring` 키프레임(박스섀도 확산)으로 펄스 애니메이션 도트 + "전액 정상 납입" 텍스트.
- **본문 리스트**: 연월/금액/상태 3열 헤더 추가, 각 행은 점선 구분선, 금액은 볼드 강조, 상태는 초록 배지. 각 행에 `.receipt-row` 클래스를 추가하고 `index.css`에 `:hover` 시 옅은 회색 배경으로 바뀌는 규칙을 새로 정의(인라인 스타일로는 `:hover`를 표현할 수 없어 별도 CSS 클래스로 처리).
- Playwright로 실제 화면을 렌더링해 최종 디자인을 확인.

---

### 18. (참고 이미지 첨부) "첨부한 사진처럼 각 납입내역을 클릭하면 상세화면 창이 뜨게해줘 내역복사 버튼은 필요없어"
`frontend/src/App.jsx` 수정. 영수증 카드의 각 행을 클릭하면 납입 상세 정보 모달이 뜨도록 신규 추가(기존 다른 모달들과 같은 중앙 다이얼로그 패턴 재사용).
- **데이터**: 행 클릭 시 선택된 결제 건(`s.paymentDetail`)을 저장하고, 승인번호·결제 일시·결제 수단·공급가액·부가가치세를 그 결제 건의 연월·금액으로부터 파생 계산(`paymentDetailView`). 공급가액/부가세는 금액을 10% 부가세 기준으로 역산.
- **모달 UI**: 첨부 이미지와 동일한 구성(아이콘+"납입 상세 정보" 타이틀+닫기, 연월 배지, 큰 금액, 초록 체크+"정상 완납", 승인번호/결제일시/결제수단/공급가액/부가가치세 목록) — 색상은 이미지의 보라색 대신 앱의 브랜드 그린으로 통일.
- 사용자 요청대로 **"내역 복사" 버튼은 제외**하고 "확인" 버튼만 배치.
- Playwright로 실제 클릭 동작까지 확인.

---

### 19. (스크린샷 첨부) "왜 중간에 잘리지? 잘린 부분은 더보기 버튼을 통해 나머지가 열리는 방식으로 보이게 해줘. 사용자가 연도별로 납입내역을 확인할 수 있게해줘"
`frontend/src/App.jsx` 수정. (확인해보니 실제로 잘린 게 아니라 카드가 화면 스크롤 영역보다 길어서 스크롤해야 나머지가 보이는 정상 상태였음 — 이를 스크롤 대신 명시적인 "더보기" 조작으로 바꿔달라는 요청.)
- **연도별 필터**: 납입 내역에서 연도를 추출해 "2026년/2025년" 같은 칩을 요약 영역 아래에 추가. 연도를 클릭하면 그 해 내역만 표시되고, "총 납입 합계"도 전체 누적이 아니라 **선택한 연도의 합계**로 재계산되도록 변경(리스트에 보이는 내용과 합계가 항상 일치하도록).
- **더보기/접기**: 연도별로 처음엔 6건만 보여주고, 그 이상이면 "더보기 (N건) ›" 버튼이 나타나 누르면 해당 연도의 나머지 내역이 전부 펼쳐지며 버튼이 "접기 ‹"로 바뀜(하나의 버튼이 상태에 따라 라벨을 바꾸는 기존 패턴 재사용). 연도를 바꾸면 더보기 상태는 자동으로 초기화됨.
- Playwright로 접기/더보기/연도 전환 3가지 상태를 모두 스크린샷으로 확인.

---

### 20. (스크린샷 첨부) "영수증 끝이 갑자기 끊긴 느낌인데 확인해줘. 접기와 더보기 버튼도 안 보여"
`frontend/src/App.jsx` 수정. Playwright를 390×844(아이폰 크기) 실제 폰 화면으로 재현해보니 실제 버그였음을 확인.
- **원인**: 영수증 카드가 들어있는 부모가 `flex-direction:column` 스크롤 컨테이너인데, 카드 자체(`overflow:hidden` 적용됨)에 이전 "입력 수정"/"로그아웃" 버튼과 동일하게 `flex:none`이 빠져 있었음. 콘텐츠가 길어지자 flex가 카드를 화면에 맞게 강제로 찌그러뜨렸고, `overflow:hidden` 때문에 넘치는 내용(4번째 줄부터 더보기 버튼까지)이 스크롤도 안 되고 그냥 잘려서 사라졌던 것.
- **수정**: 영수증 카드 컨테이너에 `flex:none` 추가.
- Playwright로 수정 전/후 비교 스크린샷을 찍어 6줄 전체와 "더보기" 버튼, 카드 하단 둥근 모서리까지 정상적으로 보이는 것을 확인.

---

### 21. (mockup HTML 파일 첨부: 보험금청구_화면목업_v5.html) "피보험자 학생이 수능을 보는날이 지나면 홈화면에서 보험료에 대해 안내해주는 내용이 모두 사라지고 자동으로 보험금 청구를 할 수 있는 화면으로 바뀌었으면 좋겠어. 보험금 청구하는 화면은 첨부한 html과 최대한 동일하게 만들어줘"
`frontend/src/App.jsx`, `frontend/src/index.css` 수정. 대규모 신규 기능 — 수능일 이후 홈 화면이 "보험료 안내"에서 "보험금 청구" 플로우로 자동 전환되도록 mockup(약 13개 화면)을 포팅.
- **사전 확인**: 시작 전 (1) 수능일 판정 기준을 실제 오늘 날짜와 하드코딩된 수능일 비교로 할지, (2) mockup 전체를 다 포팅할지 핵심 흐름만 할지 사용자에게 확인받음 → "하드코딩 수능일 + 실제 날짜 비교", "mockup 전체를 최대한 그대로"로 결정.
- **한글 인코딩 이슈**: 첨부된 mockup HTML의 한글 텍스트가 깨져서(mojibake) 전달됨. Python으로 여러 인코딩 조합(Latin-1, cp1252 등)으로 복원을 시도했으나 일부만 성공하고 나머지는 완전히 복구 불가능함을 확인 → 구조·CSS·로직(변수명, 클래스명, 계산식)은 100% 그대로 포팅하고, 한글 문구는 이 앱에 이미 쓰이던 보험 용어 톤에 맞춰 새로 작성(사용자에게 이 점 안내).
- **전환 로직**: `EXAM_DATE`(2026.11.19, 하드코딩) 대비 `new Date()`가 지났는지로 `examPassed()` 판정. 지나면 홈 화면(`ins01`)의 보험료 카드·왜 이 금액·월별 납입 영수증이 전부 사라지고 청구 진입 카드로 대체됨.
- **청구 도메인**: mockup의 FORMS/CARDS/COSTS/ROUND_META/engine() 로직을 그대로 이식(`CLAIM_LIMITS`, `CLAIM_CARDS_META`, `CLAIM_COSTS_META`, `CLAIM_ROUND_META`, `claimEngine()` 등 새 메서드). 지급액 = min(누적 인정비용×70%, 연간 한도) 계산 로직까지 동일.
- **포팅한 화면(13개)**: 홈 진입 상태카드(정상/대기/소멸/비대상 4종), 보장 자격 상세(SVG 하락 그래프 포함), 결제 동기화 로딩, 카드 연동, 카드 연결중 로딩, 청구 안내, STEP1(비용 증빙)/STEP2(받는 방법)/STEP3(지급액 확인), 제출중 로딩, 청구 완료, 진행 상황(타임라인), 청구창 대기(D-day), 자격 소멸(최종 정산).
- **검증**: Playwright에 `Date`를 오버라이드하는 초기화 스크립트를 주입해 "수능 이후" 시점을 재현한 뒤, 로그인 → 자격상세 → 카드연동 → 카드연결 → STEP1~3 → 제출 → 완료 → 진행상황까지 전체 플로우를 실제로 클릭하며 12장의 스크린샷으로 확인. 지급액 계산 결과가 mockup의 예시 수치(1차 기지급 3,640,000원, 이번 지급 4,760,000원 등)와 정확히 일치함을 확인. '비대상' 시나리오로 임시 전환해 분기도 별도 검증 후 기본값(정상/2차/중증)으로 복원.

---
