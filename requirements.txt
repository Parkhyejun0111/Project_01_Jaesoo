# Vercel Python 런타임이 읽는 의존성 목록. pyproject.toml 의 기본 dependencies 와
# 동일하게 유지할 것 (Vercel 은 빌더 버전에 따라 둘 중 하나를 사용한다).
#
# 인덱스 생성용 패키지(pypdf, langchain-*)는 여기 넣지 말 것 — 서버에서 쓰지 않는다.
# 인덱스 갱신은 로컬에서: uv sync --extra index && python build_index.py
anthropic>=0.69.0
fastapi>=0.115.0
numpy>=2.0.0
python-dotenv>=1.2.2
uvicorn>=0.34.0
voyageai>=0.3.0
