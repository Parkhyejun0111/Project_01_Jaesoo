{
  "name": "jaesoo-frontend-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "재수없수 — 인강사이트 임베디드 보험 웹 프론트엔드 (랜딩 → 보험소개 → 상품선택 → 약관 → 청약서 → 가입완료·앱다운로드)",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "prebuild": "node ../scripts/sync-policy.mjs && node ../scripts/check-assets.mjs ../web/public",
    "predev": "node ../scripts/sync-policy.mjs"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "@jaesoo/api-client": "*"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^5.4.10"
  }
}
