# 의존성
node_modules/
.venv/
__pycache__/
*.pyc

# 빌드 산출물
dist/
.next/
out/
*.tsbuildinfo

# 환경변수 — 절대 커밋하지 않는다
.env
.env.*
!.env.example

# 런타임 데이터 (업로드 파일에는 개인정보가 포함될 수 있다)
api/uploads/
api/.data/
*.sqlite
*.db

# 지역계수 배치 로그. 계수 JSON(config/*.json) 자체는 폴백 캐시이므로 커밋한다.
config/collect_log.jsonl
api/config/collect_log.jsonl

# 도구·OS
.DS_Store
.vscode/
.idea/
*.log
.wrangler/

# Vercel 링크·OIDC 토큰
.vercel/
